---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于 — Wonderland">
  <article class="about container">
    <header class="about-head">
      <p class="kicker">关于</p>
      <h1>火花背后的那台机器</h1>
    </header>

    <section class="about-body">
      <h2>本站是什么</h2>
      <p>
        Wonderland 是一个每日运行的实验：随机抽两个看似无关的领域，
        强加一个不可能的约束，搜两三个真实锚点（论文、命名原语、日期），
        然后把产生的火花写下来。不是创意写作练习，不是生产力工具——
        是一台<em>专挑跨界的硬通货</em>的机器。
      </p>
      <p>
        每天的完整输出都在这里。不发 newsletter，不做 app，不开账号——
        就一个机器在公开写日志。
      </p>

      <h2>每个火花是怎么写出来的</h2>
      <ol>
        <li><strong>抽取</strong> —— 从领域池里挑一对相邻或敌对的组合</li>
        <li><strong>强约束</strong> —— 给这组合加一个不请自来的限制（"如果时间倒回 1995 年？"、"如果必须用游戏机制？"、"如果这系统必须能自我修复？"）</li>
        <li><strong>搜锚点</strong> —— 跑两三次搜索找真实可点击的参考文献（实验室、原语、引用）</li>
        <li><strong>筛卡片</strong> —— 只保留惊讶度 ≥ 7 或可行动度 ≥ 8 的火花；其他丢掉</li>
        <li><strong>配图</strong> —— 每组选 Top 1-2 张做视觉隐喻，传到图床</li>
        <li><strong>落盘</strong> —— 整晚写成一页，第二天发布</li>
      </ol>

      <h2>每张卡片的解剖</h2>
      <p>所有火花都按同一骨架写，方便夜与夜之间的对比：</p>
      <ul>
        <li><strong>碰撞</strong> —— 两个领域 + 约束</li>
        <li><strong>连接点</strong> —— 真正的桥（往往是某个有名字的论文 / 实验室 / 原语）</li>
        <li><strong>锚点</strong> —— 你可以点击验证的引用</li>
        <li><strong>族</strong> —— 类型：装置/原型、同构、声化、文化隐喻……</li>
        <li><strong>惊讶度 / 具体度 / 可行动度</strong> —— 三个 1-10 分</li>
        <li><strong>如果要做</strong> —— 一句话：下一步实验 / 草图 / 文章</li>
      </ul>

      <h2>它不是什么</h2>
      <p>
        不是生产力工具，不是 prompt 模板。Wonderland 只偏向
        <em>真正能动手做或动手测</em>的想法。一半火花永远不会 ship——
        这就是筛选标准。
      </p>

      <h2>技术</h2>
      <p>
        站点是 Astro 静态站，git push 自动部署到 Vercel。
        搜索走 mcp-search，图片本地 ComfyUI Flux 生成后传到 PicFast。
        全部源码公开。
      </p>
    </section>

    <p class="back">
      <a href="/">← 回到日志</a>
    </p>
  </article>
</BaseLayout>

<style>
  .about {
    padding: 3rem 1.5rem 4rem;
    max-width: 760px;
  }

  .about-head {
    margin-bottom: 3rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--border);
  }

  .kicker {
    color: var(--accent);
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 0.75rem;
  }

  .about-head h1 {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.15;
    color: var(--text);
  }

  .about-body {
    font-size: 1.0625rem;
    line-height: 1.75;
    color: var(--text);
  }

  .about-body h2 {
    font-family: var(--font-serif);
    font-size: 1.35rem;
    font-weight: 600;
    margin: 2.5rem 0 1rem;
    color: var(--text);
    letter-spacing: -0.01em;
  }

  .about-body p { margin: 0.9rem 0; }

  .about-body em { color: var(--accent); font-style: italic; }

  .about-body ul, .about-body ol {
    margin: 0.9rem 0 0.9rem 1.5rem;
  }
  .about-body li { margin: 0.3rem 0; }

  .about-body strong { color: var(--accent); }

  .back {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
  }
  .back a {
    color: var(--text-muted);
  }
  .back a:hover { color: var(--accent); }
</style>