---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于 — Wonderland">
  <article class="about container">
    <header class="about-head">
      <p class="kicker">关于</p>
      <h1>关于此站点</h1>
    </header>

    <section class="about-body">
      <h2>本站是什么</h2>
      <p>
        Wonderland 是一个跨领域灵感的常态化实验：每日遴选两个看似无关的领域，
        引入一项约束命题，检索相关文献锚点，而后记录所产生的火花。
        此处仅收录火花本身及其参考文献——不含创作过程、不含营销内容、
        不含订阅入口。
      </p>

      <h2>一期的工作流程</h2>
      <ol>
        <li><strong>遴选</strong> —— 从领域池中抽取两个相邻或对偶的领域</li>
        <li><strong>约束命题</strong> —— 引入一项不请自来的限制（例如：若数据规模放大 10⁴ 倍？若该系统必须具备自修复能力？）</li>
        <li><strong>文献锚定</strong> —— 对每组碰撞执行二至三次检索，确认真实可引用的参考文献</li>
        <li><strong>筛选</strong> —— 仅保留惊讶度 ≥ 7 或可行动度 ≥ 8 的火花；其余舍弃</li>
        <li><strong>插图</strong> —— 每组遴选 Top 1-2 张火花生成视觉隐喻，并托管于公开图床</li>
        <li><strong>归档</strong> —— 整期汇为一页，次日发布</li>
      </ol>

      <h2>火花卡片的标准结构</h2>
      <p>所有火花均按统一骨架记录，以便跨期对照：</p>
      <ul>
        <li><strong>碰撞</strong> —— 两个领域 + 约束命题</li>
        <li><strong>连接点</strong> —— 真正的结构桥梁（通常引用某篇论文 / 实验室 / 命名原语）</li>
        <li><strong>锚点</strong> —— 可点击验证的参考文献</li>
        <li><strong>族</strong> —— 分类：装置/原型、同构、声化、文化隐喻……</li>
        <li><strong>惊讶度 / 具体度 / 可行动度</strong> —— 三项 1-10 评分</li>
        <li><strong>如果要做</strong> —— 一句话：指明下一步的实验 / 草图 / 论述</li>
      </ul>

      <h2>甄选标准</h2>
      <p>
        偏好真正可以动手实现或动手验证的想法。
        其中相当部分永远不会落地——这属于甄选过程的固有特性。
        任何在脱离检索结果的前提下即可推断的命题，
        将被视作陈词滥调而舍弃。
      </p>

      <h2>技术架构</h2>
      <p>
        本站采用 Astro 静态构建，源代码托管于 GitHub，部署由 Vercel 自动接管。
        搜索由 mcp-search 提供，图像经本地 ComfyUI Flux 生成后托管于 PicFast。
      </p>
    </section>

    <p class="back">
      <a href="/">← 返回日志</a>
    </p>
  </article>
</BaseLayout>

<style>
  .about {
    padding: 3rem 1.5rem 4rem;
    max-width: 760px;
  }

  .about-head {
    margin-bottom: 3rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--border);
  }

  .kicker {
    color: var(--accent);
    font-size: 0.8125rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 0.75rem;
  }

  .about-head h1 {
    font-family: var(--font-serif);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.15;
    color: var(--text);
  }

  .about-body {
    font-size: 1.0625rem;
    line-height: 1.75;
    color: var(--text);
  }

  .about-body h2 {
    font-family: var(--font-serif);
    font-size: 1.35rem;
    font-weight: 600;
    margin: 2.5rem 0 1rem;
    color: var(--text);
    letter-spacing: -0.01em;
  }

  .about-body p { margin: 0.9rem 0; }

  .about-body em { color: var(--accent); font-style: italic; }

  .about-body ul, .about-body ol {
    margin: 0.9rem 0 0.9rem 1.5rem;
  }
  .about-body li { margin: 0.3rem 0; }

  .about-body strong { color: var(--accent); }

  .back {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
  }
  .back a {
    color: var(--text-muted);
  }
  .back a:hover { color: var(--accent); }
</style>